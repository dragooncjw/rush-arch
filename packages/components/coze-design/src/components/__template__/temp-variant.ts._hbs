import { cva, type VariantProps } from 'class-variance-authority';

const {{camelName}}Variants = cva(['coz-{{name}}'], {
  variants: {
    size: {
      small: ['text-base'],
      default: ['text-lg'],
      large: ['text-xl'],
    },
  },
  compoundVariants: [],
  defaultVariants: {
    size: 'default',
  },
});

export type {{pascalName}}Props = VariantProps<typeof {{camelName}}Variants>;

export { {{camelName}}Variants };
