export const DEFAULT_VALUE = `
Cosplay the role {#Slot placeholder="role name" mode="input"#}WuKong{#/Slot#}

{{valid}} {{invalid}}

Use {#Tool id="123"#}Web Search{#/Tool#} if user asks for it.

Use {#Tool id="456"#}Weather Query{#/Tool#} if user asks for it.
`.trim()
